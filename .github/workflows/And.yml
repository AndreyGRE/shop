name: 🚀 Deploy Next.js via SSH

on:
  push:
    branches: [ master, main ] 
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 🧾 Checkout repository 
      uses: actions/checkout@v4
    - name: 🚀 Deploy to server via SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          echo "🔗 Подключаемся к серверу..."
          cd /var/www/html/shop

          # Разрешаем git работать в этой директории
          git config --global --add safe.directory /var/www/html/shop

          echo "📥 Получаем свежий код..."
          git fetch origin master
          git reset --hard origin/master

          echo "🧹 ПОЛНАЯ очистка старых сборок и кеша..."
          rm -rf node_modules .next package-lock.json
          rm -rf .next/cache          # очистка кэша Next.js
          npm cache clean --force

          # Устанавливаем правильные права (root владеет всем)
          chown -R root:root /var/www/html/shop

          echo "📦 Чистая установка зависимостей..."
          npm install --legacy-peer-deps

          echo "🔨 Собираем проект..."      

          npx next build 
         
          # Используем PM2 для управления процессом
          pm2 delete next-app 2>/dev/null || true

          # Загружаем переменные и запускаем
          export NODE_ENV=production
          export YOOKASSA_SHOP_ID='${{ secrets.YOOKASSA_SHOP_ID }}'
          export YOOKASSA_SECRET_KEY_TEST='${{ secrets.YOOKASSA_SECRET_KEY_TEST }}'


          pm2 start npm --name "next-app" -- start
          pm2 save


          echo "✅ Деплой завершен!"
          pm2 status