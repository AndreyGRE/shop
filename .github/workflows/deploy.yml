name: 🚀 Deploy ColorfullShop

on:
  push:
    branches: [ es ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: ⚙️ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: 📦 Install dependencies
      run: npm ci
    
    - name: 🔨 Build project
      run: npm run build
    
    - name: 📁 Create deployment package
      run: |
        mkdir -p deployment
        # Копируем файлы для продакшена
        cp -r .next package.json package-lock.json next.config.js* public deployment/
        cp -r .env* deployment/ 2>/dev/null || true
    
    - name: 🚀 Deploy to server
      uses: appleboy/scp-action@v0.1.4
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        source: "deployment/"
        target: "/var/www/html"
        strip_components: 1
    
    - name: 🎯 Setup and start on server
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          cd /var/www/html
          
          # Устанавливаем зависимости
          npm ci --production
          
          # Устанавливаем PM2 если не установлен
          if ! command -v pm2 &> /dev/null; then
            npm install -g pm2
          fi
          
          # Останавливаем предыдущий процесс
          pm2 stop colorfullshop || true
          pm2 delete colorfullshop || true
          
          # Запускаем Next.js приложение
          pm2 start npm --name "colorfullshop" -- start
          pm2 save
          
          echo "✅ Deploy completed successfully!"