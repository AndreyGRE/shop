name: 🚀 Deploy ColorfullShop

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: ⚙️ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: 📦 Install dependencies
      run: npm ci
    
    - name: 🔨 Build project
      run: npm run build
    
    - name: 📁 Create deployment package
      run: |
        mkdir -p deployment
        # Копируем файлы для продакшена
        cp -r .next package.json package-lock.json next.config.js* public deployment/
        cp -r .env* deployment/ 2>/dev/null || true
    
    - name: 🚀 Deploy to server
      uses: appleboy/scp-action@v0.1.4
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        source: "deployment/"
        target: "/var/www/html"
        strip_components: 1
    
    - name: 🎯 Setup and restart on server
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          cd /var/www/html
          
          # Устанавливаем зависимости
          npm ci --production
          
          # Перезапускаем существующий процесс next-app (а не colorfullshop)
          pm2 restart next-app
          
          echo "✅ Deploy completed successfully!"
          echo "📊 Application status:"
          pm2 status